<h3>All MY Classes </h3>

<ul>
    <% @klasses.each do |klass| %>
        <% if klass.teacher_id == current_teacher.id %>
            <li> <%= link_to klass.name, klass_path(klass) %> </li>
        <% end %>
    <% end %>
</ul>